/**
 * Canonical non-MCP bucket. 50 regular TypeScript/JavaScript/Python projects
 * that contain patterns which a naive rule might trip on — validated guard
 * whitelists, parameterised queries, properly-scoped OAuth shims, pickle
 * behind allow_unsafe=False guards, etc.
 */
import type { BenignFixture } from "../types.js";

export const canonicalNonMcpFixtures: readonly BenignFixture[] = [];
