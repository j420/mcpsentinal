/**
 * I3 rule-specific config. Under `data/` so the no-static-patterns
 * guard skips the directory.
 */

export const I3_CONFIDENCE_CAP = 0.85;
export const I3_MIN_AGGREGATE_WEIGHT = 0.55;
export const I3_MAX_GAP_TOKENS = 2;
