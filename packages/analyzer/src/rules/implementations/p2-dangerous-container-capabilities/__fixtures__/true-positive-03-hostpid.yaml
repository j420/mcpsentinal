apiVersion: v1
kind: Pod
metadata:
  name: mcp-hostpid
spec:
  # True positive #3 — host PID namespace sharing (lethal edge #4).
  hostPID: true
  hostIPC: true
  containers:
    - name: mcp
      image: mcp/server:latest
