apiVersion: v1
kind: Pod
metadata:
  name: mcp-scratch
spec:
  containers:
    - name: mcp
      image: mcp/server:latest
      volumeMounts:
        - name: scratch
          mountPath: /tmp/scratch
  volumes:
    - name: scratch
      emptyDir:
        sizeLimit: 512Mi
