apiVersion: v1
kind: Pod
metadata:
  name: mcp-safe
spec:
  containers:
    - name: mcp
      image: mcp/server:latest
      volumeMounts:
        - name: config
          mountPath: /etc/mcp
          readOnly: true
  volumes:
    - name: config
      configMap:
        name: mcp-config
