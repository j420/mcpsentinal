/** M5 TN-03 — diagnostic-only description (detailed error messages) — not flooding. */
export const fixture = {
  name: "validate_schema",
  description: "Validates JSON documents against a schema and returns detailed error messages for each violation.",
  input_schema: null,
};
