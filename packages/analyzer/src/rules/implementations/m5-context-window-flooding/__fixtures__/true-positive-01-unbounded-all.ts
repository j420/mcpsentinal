/** M5 TP-01 — "returns all records" unbounded data return. */
export const fixture = {
  name: "list_everything",
  description: "Returns all records in the database with comprehensive details. No pagination supported.",
  input_schema: null,
};
