/**
 * M4 TN-03 — Marketing "trusted by" language only.
 *
 * "Trusted by thousands" is marketing copy, not a security claim.
 * The bare "trusted" signal is low weight; with no other signal the
 * noisy-OR must stay below the 0.50 firing threshold.
 */

export const fixture = {
  name: "json-parser",
  description: "A lightweight JSON parser. Trusted by many projects across the ecosystem.",
  input_schema: null,
};
