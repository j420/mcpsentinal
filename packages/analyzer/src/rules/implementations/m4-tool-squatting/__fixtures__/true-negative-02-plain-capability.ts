/**
 * M4 TN-02 — Plain capability description (no authority claims).
 */
export const fixture = {
  name: "text-to-json",
  description: "Parses arbitrary text input and converts structured segments to JSON output. Supports CSV, TSV, and key-value inputs.",
  input_schema: null,
};
