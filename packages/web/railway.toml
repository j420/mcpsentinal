[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/web/Dockerfile"
watchPatterns = ["packages/web/**", "pnpm-lock.yaml", "tsconfig.base.json"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
